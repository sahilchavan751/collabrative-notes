export default function NotesListPage() {
  // On mobile, the AuthenticatedLayout handles rendering the SecondarySidebar (note list)
  // instead of this component when the pathname is "/notes".
  return null;
}
